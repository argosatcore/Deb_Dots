#!/bin/sh

CHOICE=$(echo "Shutdown\nReboot" | wofi --show=dmenu -p "Power Menu" --width=20% --lines=2 --xoffset=548 --yoffset=200)

case "$CHOICE" in 
	Shutdown) systemctl poweroff & ;;
	Reboot) systemctl reboot & ;;
esac
		
